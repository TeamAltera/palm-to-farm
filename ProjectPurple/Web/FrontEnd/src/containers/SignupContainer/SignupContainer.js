import React, { Component } from "react";
import { LoginWrapper, SignForm } from "../../components";

export default class LoginContainer extends Component {
  render() {
    return (
      <LoginWrapper>
        <SignForm />
      </LoginWrapper>
    );
  }
}
